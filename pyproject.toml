[project]
name = "open-plan-docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.3",
    "tabulate>=0.10.0",
]

[dependency-groups]
dev = [
    "zensical>=0.0.45",
]
